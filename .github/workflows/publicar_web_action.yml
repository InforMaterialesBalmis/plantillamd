name: Convert Markdown to PDF and Upload to Google Drive

on:
  push:
    branches:
      - main

jobs:
  build-convert-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

    #   - name: Install Pandoc
    #     run: |
    #       sudo apt-get update
    #       sudo apt-get install -y pandoc

    #   - name: Convert Markdown to DOCX
    #     run: pandoc ./ejemplos/manual.md -o ./ejemplos/manual.docx --metadata title="Título del Documento"

      - name: Upload PDF to Google Drive
        uses: willo32/google-drive-upload-action@v1
        with:
            target: ./ejemplos/manual.pdf
            credentials: ${{ secrets.GDRIVE_CREDENTIALS }}
            parent_folder_id: ${{ secrets.GDRIVE_FOLDER_ID }}
